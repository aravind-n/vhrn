# Rust proxy contract coverage ledger

This is the active ownership ledger for the frozen
[consumer contract](../../../proxy/consumer-contract.md). Each requirement group has exactly one
owning phase. Ownership changes require an explicit planning decision; implementation evidence
belongs in the owning phase file.

Only [Phase 12](phase-12-qualification.md) is required to read this ledger during normal numbered
phase execution. Phase 12 records named qualification evidence in the optional final column; other
phase agents rely on their own specifications.

| Frozen contract requirement group | Sole owner | Qualification evidence |
| --- | --- | --- |
| Component-specific CI ownership for the CLI, isolated Rust candidate, and shipping Go proxy during migration, with an explicit cutover rename/removal path | Phase 2 | `actionlint` passed the three changed/calling workflows; `ci.yml` still selects distinct `test`, `build-cli`, `build-proxy-rs`, and `build-proxy-go` jobs, and the candidate build has no registry credentials or push. |
| Host-owned public and local policy, capability separation, strict public file grammar, exact mode grammar, 1 MiB and regular-file bounds, live replacement, and fail-closed decision snapshots | Phase 3 | `strict_public_storage_corpus_and_line_framing`, `bounded_open_accepts_one_mib_and_rejects_one_byte_more`, `same_reader_observes_atomic_repair_between_persistent_requests`, and process `policy_modes_and_live_replacement_are_observed_per_request`. |
| Strict three-layer local policy grammar, canonical stored authorities, union semantics, live revocation, and failure to an empty local decision | Phase 3 | `strict_local_storage_is_distinct_from_user_input_normalization`, `bad_local_layer_empties_the_union_and_repair_is_live`, and process `local_policy_is_rechecked_before_every_broker_connection`. |
| Environment resolution, empty-as-unset behavior, plural/singular precedence, exact local all-or-none group, listener validation, and secret-safe configuration errors | Phase 4 | `resolves_startup_corpus`, `resolves_allowlist_and_local_edges`, `empty_values_are_unset_for_every_default_and_optional_variable`, `every_partial_nonempty_local_group_is_rejected`, and `invalid_listener_values_are_rejected_without_echoing_values`. |
| Startup ordering and readiness: initial policy checks, append check, bound-but-not-serving listener, token load, broker `READY`, and nonzero failure | Phase 4 | Process `invalid_startup_policy_log_and_token_exit_redacted`, `listener_does_not_serve_until_broker_readiness_and_refusal_is_fatal`, `broker_readiness_timeout_is_fatal_and_redacted`, and `nonregular_token_fails_promptly_without_serving`. |
| Denial diagnostics and append-only records, canonical record target, RFC 3339 `Z` timestamp, non-interleaving writes, report-mode append failure, and sticky log health | Phase 4 | `denial_output_appends_exact_bytes_and_disabled_is_typed`, `concurrent_appends_are_complete_and_never_truncate`, `report_mode_audit_write_failure_is_503_and_never_dials`, and process `health_tracks_live_policy_and_sticky_audit_failure`. |
| Raw request-target classification, DNS/IP normalization, explicit loopback classification, mandatory CONNECT port, absolute-form HTTPS rejection, and removal of proxy-originated TLS | Phase 5 | `accepted_target_truth_table_is_canonical_and_disjoint`, `connect_requires_one_explicit_canonicalizable_port`, `local_spellings_normalize_before_public_classification`, and `malformed_absolute_forms_do_not_fall_back_to_direct_or_connect`. |
| Policy authorization ordering and safe response taxonomy for syntax and policy outcomes, including exact denial and HTTPS bodies and HEAD suppression | Phase 5 | `failures_have_exact_status_headers_and_bytes_including_head`, `malformed_and_https_targets_have_no_authorization_or_network_side_effects`, `invalid_public_policy_is_an_exact_enforced_denial_before_body_polling`, and process `direct_endpoints_and_denial_log_follow_corpus`. |
| HTTP/1.0 and HTTP/1.1 ingress, Host rules, request-line/header/framing validation, slow-head timeout, upgrade rejection, and bounded incremental parsing | Phase 6 | Process `http1_ingress_and_direct_endpoint_contract_is_exact`, `http1_malformed_hosts_versions_and_prefaces_are_rejected`, and `http1_persistence_connect_framing_and_upgrade_are_enforced_before_broker_work`, plus `incomplete_head_closes_thirty_seconds_after_its_first_octet`. |
| Origin-form endpoints, `OPTIONS *`, endpoint method rules, exact health/status bodies and content types, and policy/log-sensitive health | Phase 6 | Process `http1_ingress_and_direct_endpoint_contract_is_exact`, `direct_endpoints_and_denial_log_follow_corpus`, and `health_tracks_live_policy_and_sticky_audit_failure`. |
| Reviewed 2025-10-09 IANA global-unicast boundary, literals, most-specific exceptions, IPv4-mapped/scoped rejection, and address-safety denial | Phase 7 | `runtime_registry_is_the_complete_reviewed_fixture`, `every_registry_prefix_boundary_matches_the_reviewed_fixture`, `classifies_the_address_fixture`, and `mapped_and_scoped_connect_literals_are_audited_403_without_network_work`. |
| One pinned DNS answer set, 1..=64 all-address validation, one cumulative ten-second DNS/TCP deadline, numeric fallback, no ambient proxy, and typed DNS/dial outcomes | Phase 7 | `resolves_once_and_retains_the_validated_answer_set`, `dns_answer_count_accepts_one_through_sixty_four_only`, `resolution_and_every_dial_share_one_deadline`, `literal_skips_resolution_and_uses_the_same_boundary`, and `proxy_command_removes_every_proxy_environment_variable`. |
| Broker token secrecy, exact bounded frames, three-second readiness, fresh broker connections, retained post-`OK` bytes, broker outcome typing, and no direct loopback dial | Phase 8 | `readiness_and_every_connect_use_fresh_exact_frames`, `fragmented_success_and_all_coalesced_payload_bytes_are_preserved`, `typed_errors_never_render_secret_or_endpoint`, and process `local_connect_preserves_buffered_bytes_and_survives_revocation`. |
| Local connection authorization and recheck flow, distinct `localhost`/IPv4/IPv6 grants, local HTTP connection reuse boundary, and supported engine route assumptions | Phase 8 | Process `local_policy_is_rechecked_before_every_broker_connection` and `local_http_forwards_and_streams_through_authenticated_broker`, plus CLI `numeric_and_localhost_grants_stay_distinct_with_ipv6_fallback` and `apple_gateway_route_uses_the_default_network_gateway`. |
| Forwarded request transformation: method and target, regenerated `Host`, hop-by-hop removal, `Via`, no forwarding identity headers, and no forbidden retry/cache behavior | Phase 9 | `request_line_uses_origin_form_options_star_and_canonical_host`, `forwards_expect_informationals_hop_filtering_via_and_trailers`, `removes_connection_nominated_and_fixed_hop_headers`, and `post_origin_failure_is_not_automatically_retried`. |
| Unlimited-size streaming HTTP bodies, 64 KiB directional buffers, chunked bodies and trailers, informational responses, `Expect: 100-continue`, bodyless responses, response-head validation, cancellation, and safe pooling | Phase 9 | `streams_request_larger_than_former_cap_with_aggregate_buffer_bound`, `streams_response_larger_than_former_cap_before_origin_finishes`, `forwards_expect_informationals_hop_filtering_via_and_trailers`, `head_204_and_304_preserve_describing_headers_without_body_bytes`, and process `local_http_forwards_and_streams_through_authenticated_broker`. |
| CONNECT success/failure sequencing, exact success head, eager client and broker bytes, opaque relay, 64 KiB buffers, bidirectional half-close, tunnel lifetime, and revocation semantics | Phase 10 | `connect_success_head_is_exact_and_has_no_framing_fields`, `eager_prefixes_are_first_and_lossless_in_both_directions`, `upstream_eof_keeps_client_write_half_open`, and process `local_connect_preserves_buffered_bytes_and_survives_revocation`. |
| Process-wide client/upstream limits, at-least-128 client support, bounded task and queue ownership, overload response, and idle-pool accounting | Phase 11 | Process `process_supports_one_hundred_twenty_eight_established_clients`, plus `production_admission_rejects_the_two_hundred_fifty_seventh_connection`, `production_upstream_admission_never_exceeds_two_hundred_fifty_six`, and `pooled_upstream_permits_survive_idle_and_release_on_eviction_and_close`. |
| Two-stage signal shutdown, shutdown health, cancellation of pending work, five-second drain then forced close, exit status, and idempotent host cleanup | Phase 11 | Process `sigterm_drains_then_forces_an_established_tunnel_without_policy_mounts`, `second_sigterm_forces_idempotent_shutdown_and_exits_zero`, and `cancellation_after_bind_closes_listener_and_pending_broker_exchange`, plus CLI `sigterm_stops_client_agent_proxy_broker_then_policy`. |
| Candidate image/process contract, static scratch and numeric user posture, read-only-root operation, exact mounts/env/port, multi-platform equivalence, dependency audit, and black-box contract trace | Phase 12 | All 26 `proxy_process` cases passed against `target/release/vhrn-proxy` via `VHRN_PROXY_TEST_BIN`; the inline candidate-image smoke passed one-layer/no-shell metadata, `/vhrn-proxy`, `8080`, `65532:65532`, read-only root, `cap-drop ALL`, documented mounts, health/status, and stop; amd64 and arm64 Dockerfile builds and `cargo audit --deny warnings` passed. |
| Apple `container` and Docker-through-Colima end-to-end build/run/broker verification without publishing or replacing immutable tags | Phase 12 | Redacted engine transcripts passed build, sidecar readiness/IP, exact mount/env boundary, all four agent proxy variables, firewall pinning, public allow/deny, `localhost`/127/IPv6 grants, live revocation and mode replacement, cleanup, and SIGTERM on Apple `container` 1.4.1 and Colima 0.10.3/Docker 29.8.1; only `vhrn-proxy:rust-candidate` was used and nothing was pushed. |
| Atomic production selection, `vhrn-proxy` image and CLI release-clock tags, workspace/CI/build context, local image name, removal of the legacy implementation/toolchain, and final operator documentation | Phase 13 | Phase 13 remains the sole owner and is still blocked on explicit cutover approval. Phase 12's diff audit found no production selector, publisher, immutable tag, Go proxy, release, or version change; existing `proxy_tag_rides_cli_version` and `container_run_args_golden` remained green. |
